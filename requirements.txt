ultralytics>=8.1.0
onnx>=1.14.0
onnxsim>=0.4.33
opencv-python>=4.8.0
numpy>=1.24.0
matplotlib>=3.7.0
psutil>=5.9.0
nvidia-ml-py>=12.0.0

# ─── ONNX Runtime GPU kurulumu ───────────────────────────────────────────────
#
# ÖNEMLİ: onnxruntime-gpu sadece CUDA 12.x'i destekler (Nisan 2026 itibarıyla).
# torch cu130 (CUDA 13.0) kullanıyorsan aşağıdaki seçenekler geçerli:
#
# SEÇENEK A — Stabil (önerilen): PyTorch cu121 + onnxruntime-gpu 1.20.x
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   pip install onnxruntime-gpu==1.20.1
#
# SEÇENEK B — Nightly CUDA 13 (deneysel):
#   pip install flatbuffers numpy packaging protobuf sympy
#   pip install --pre --index-url \
#     https://aiinfra.pkgs.visualstudio.com/PublicPackages/_packaging/ort-cuda-13-nightly/pypi/simple/ \
#     onnxruntime-gpu
#
# SEÇENEK C — Mevcut PyTorch'u koru, ORT GPU'dan vazgeç:
#   pip install onnxruntime   (CPU-only; benchmark CPU sonucu gösterir)
#
# Doğrudan pip install onnxruntime-gpu yazmak CUDA 12 binary yükler
# ve cu130 ortamında CUDAExecutionProvider çalışmaz.
# ─────────────────────────────────────────────────────────────────────────────

# TensorRT: NVIDIA resmi sitesinden kurun
# https://developer.nvidia.com/tensorrt
# veya: pip install tensorrt  (Linux + CUDA uyumu gerekli)
